<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_purchase_request_form" model="ir.ui.view">
        <field name="name">purchase.request.form</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <form string="Purchase Request">
                <header>
                    <button name="button_draft" string="Reset" type="object" invisible="state not in ('to_approve','approved','rejected','in_progress','done')" groups="purchase_request.group_purchase_request_manager" />
                    <button name="button_to_approve" string="Request approval" type="object" invisible="state not in ('draft')" class="oe_highlight" />
                    <button name="button_approved" string="Approve" type="object" invisible="state not in ('to_approve')" class="oe_highlight" groups="purchase_request.group_purchase_request_manager" />
                    <button name="button_in_progress" string="In Progress" type="object" invisible="state not in ('approved')" groups="purchase_request.group_purchase_request_manager" />
                    <button name="%(action_purchase_request_line_make_purchase_order)d" string="Create RFQ" type="action" invisible="state not in ('approved','in_progress')" />
                    <button name="button_done" string="Done" type="object" invisible="state not in ('approved','in_progress')" class="oe_highlight" groups="purchase_request.group_purchase_request_manager" />
                    <button name="button_rejected" string="Reject" type="object" invisible="state not in ('to_approve', 'approved','in_progress')" groups="purchase_request.group_purchase_request_manager" />
                    <field name="state" widget="statusbar" statusbar_visible="draft,to_approve,approved,done,in_progress" statusbar_colors='{"approved":"blue"}' />
                </header>
                <sheet>
                    <div name="button_box" class="oe_button_box">
                        <button name="action_view_purchase_request_line" type="object" class="oe_stat_button" invisible="line_count == 0" icon="fa-list">
                            <field name="line_count" string="Lines" widget="statinfo" />
                        </button>
                        <button name="action_view_purchase_order" string="Purchase Order" type="object" class="oe_stat_button" invisible="purchase_count == 0" icon="fa-shopping-cart">
                            <field name="purchase_count" string="Purchase Orders" widget="statinfo" />
                        </button>
                        <button name="action_view_stock_picking" string="Picking" type="object" class="oe_stat_button" invisible="move_count == 0" groups="stock.group_stock_manager" icon="fa-truck">
                            <field name="move_count" string="Pickings" widget="statinfo" />
                        </button>
                    </div>
                    <div class="oe_edit_only">
                        <label for="name" class="oe_inline" />
                    </div>
                    <h1>
                        <field name="is_editable" invisible="1" />
                        <field name="is_name_editable" invisible="1" />
                        <field name="name" class="oe_inline" readonly="is_name_editable == False" />
                    </h1>
                    <group>
                        <group>
                            <field name="requested_by" readonly="is_editable == False" />
                            <field name="assigned_to" readonly="is_editable == False" />
                        </group>
                        <group>
                            <field name="date_start" readonly="is_editable == False" />
                            <field name="picking_type_id" readonly="is_editable == False" />
                            <field name="company_id" invisible="1" />
                            <field name="company_id" groups="base.group_multi_company" widget="selection" readonly="1" />
                        </group>
                    </group>
                    <notebook>
                        <page string="Products">
                            <field name="line_ids">
                                <list decoration-muted="cancelled == True" editable="bottom">
                                    <field name="product_id" required="1" />
                                    <field name="name" />
                                    <field name="product_qty" />
                                    <field name="product_uom_id" />
                                    <field name="analytic_distribution" column_invisible="1" />
                                    <field name="date_required" />
                                    <field name="estimated_cost" column_invisible="1" />
                                    <field name="currency_id" column_invisible="1" />
                                    <field name="cancelled" column_invisible="1" />
                                    <field name="is_editable" column_invisible="1" />
                                    <field name="purchased_qty" column_invisible="1" />
                                    <field name="purchase_state" column_invisible="1" />
                                    <button name="action_show_details" type="object" icon="fa-list" title="Show Details" width="0.1" options='{"warn": true}' />
                                </list>
                            </field>
                            <group class="oe_subtotal_footer oe_right">
                                <field name="currency_id" invisible="1" />
                                <div class="oe_subtotal_footer_separator oe_inline">
                                    <label for="estimated_cost" />
                                </div>
                                <field name="estimated_cost" nolabel="1" class="oe_subtotal_footer_separator" widget="monetary" options="{'currency_field': 'currency_id'}" />
                            </group>
                        </page>
                        <page name="other_information" string="Autres Informations">
                            <group>
                                <group>
                                    <field name="origin" class="oe_inline" readonly="is_editable == False" />
                                    <field name="description" readonly="is_editable == False" />
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_purchase_request_tree" model="ir.ui.view">
        <field name="name">purchase.request.list</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <list name="Purchase Request" decoration-bf="message_needaction==True" decoration-info="state in ('draft','to_approve')" decoration-muted="state in ('rejected')">
                <field name="message_needaction" column_invisible="1" />
                <field name="name" />
                <field name="date_start" />
                <field name="requested_by" widget="many2one_avatar_user" />
                <field name="company_id" groups="base.group_multi_company" widget="selection" />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field name="origin" />
                <field name="currency_id" column_invisible="1" />
                <field name="estimated_cost" optional="hide" />
                <field name="state" widget="badge" decoration-success="state in ('done', 'approved', 'in_progress')" decoration-muted="state == 'draft'" decoration-warning="state == 'to_approve'" decoration-danger="state == 'rejected'" />
            </list>
        </field>
    </record>

    <record id="view_purchase_request_search" model="ir.ui.view">
        <field name="name">purchase.request.list.select</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <search string="Search Purchase Request">
                <field name="name" string="Purchase Request" />
                <separator />
                <field name="product_id" />
                <field name="state" />
                <filter name="unassigned" string="Unassigned" domain="[('assigned_to','=', False)]" help="Unassigned Request" />
                <separator />
                <filter name="state_draft" string="Draft" domain="[('state','=','draft')]" help="Request is to be approved" />
                <filter name="state_to_approve" string="To Approve" domain="[('state','=','to_approve')]" help="Request is to be approved" />
                <filter name="state_approved" string="Approved" domain="[('state','=','approved')]" help="Request is approved" />
                <filter name="state_in_progress" string="In Progress" domain="[('state','=','in_progress')]" help="Request is in progress" />
                <filter name="state_rejected" string="Rejected" domain="[('state','=','rejected')]" help="Request is rejected" />
                <filter name="state_done" string="Done" domain="[('state','=','done')]" help="Request is done" />
                <separator />
                <filter name="message_needaction" string="Unread Messages" domain="[('message_needaction','=',True)]" />
                <filter name="assigned_to_me" domain="[('assigned_to','=', uid)]" help="Assigned to me" />
                <filter name="my_requests" domain="[('requested_by','=', uid)]" help="My requests" />
                <field name="requested_by" />
                <field name="assigned_to" />
                <field name="picking_type_id" />
                <filter name="activities_overdue" string="Late Activities" invisible="1" domain="[('my_activity_date_deadline', '&lt;', 'today')]" />
                <filter name="activities_today" string="Today Activities" invisible="1" domain="[('my_activity_date_deadline', '=', 'today')]" />
                <filter name="activities_upcoming_all" string="Future Activities" invisible="1" domain="[('my_activity_date_deadline', '&gt;', 'today')]" />
                <group>
                    <filter name="requested_by" string="Requested by" icon="fa-user" domain="[]" context="{'group_by':'requested_by'}" />
                    <filter name="assigned_to" string="Assigned to" icon="fa-user" domain="[]" context="{'group_by':'assigned_to'}" />
                    <filter name="source" string="Source" icon="fa-caret-square-o-left" domain="[]" context="{'group_by':'origin'}" />
                    <filter name="start_date" string="Start Date" icon="fa-calendar" domain="[]" context="{'group_by':'date_start'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="view_purchase_request_kanban" model="ir.ui.view">
        <field name="name">purchase.request.kanban</field>
        <field name="model">purchase.request</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile" quick_create="false">
                <field name="name" />
                <field name="requested_by" readonly="1" />
                <field name="estimated_cost" />
                <field name="state" />
                <field name="date_start" />
                <field name="currency_id" readonly="1" />
                <field name="activity_state" />
                <progressbar field="activity_state" colors='{"planned": "success", "today": "warning", "overdue": "danger"}' />
                <templates>
                    <t t-name="card">
                        <div class="d-flex align-items-baseline">
                            <field name="requested_by" class="fw-bolder fs-5 me-2" />
                            <field name="estimated_cost" widget="monetary" class="fw-bolder ms-auto flex-shrink-0" />
                        </div>
                        <footer class="align-items-end">
                            <div class="d-flex flex-wrap gap-1 text-nowrap">
                                <field name="name" />
                                <field name="date_start" options="{'show_time': false}" />
                                <field name="activity_ids" widget="kanban_activity" />
                            </div>
                            <field name="state" widget="label_selection" options="{'classes': {'draft': 'default', 'cancel': 'default', 'done': 'success', 'approved': 'warning'}}" class="ms-auto" />
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="purchase_request_form_action" model="ir.actions.act_window">
        <field name="name">Purchase Requests</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">purchase.request</field>
        <field name="view_mode">list,kanban,form</field>
        <field name="context">{"search_default_requested_by":uid}</field>
        <field name="search_view_id" ref="view_purchase_request_search" />
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">Click to start a new purchase request process.</p>
            <p>A purchase request is an instruction to Purchasing to procure
            a certain quantity of materials services, so that they are
            available at a certain point in time.</p>
        </field>
    </record>

    <menuitem id="menu_purchase_request_pro_mgt" parent="purchase.menu_procurement_management" action="purchase_request_form_action" />

    <menuitem id="parent_menu_purchase_request" name="Purchase Requests" sequence="22" web_icon="purchase_request,static/description/icon.png" groups="group_purchase_request_user,group_purchase_request_manager" />

    <menuitem id="menu_purchase_request" name="Purchase Requests" parent="parent_menu_purchase_request" groups="group_purchase_request_user,group_purchase_request_manager" />

    <menuitem id="menu_purchase_request_act" sequence="10" parent="menu_purchase_request" action="purchase_request_form_action" />
</odoo>