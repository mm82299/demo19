<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_config_settings_view_form_deferred_revenue" model="ir.ui.view">
        <field name="name">res.config.settings.view.form.inherit.account.deferred.revenue</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="account.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <!-- Append inside the Customer Invoices block which always exists in Community -->
            <xpath expr="//block[@id='invoicing_settings']" position="inside">
                <setting string="Deferred revenue entries:" company_dependent="1">
                    <div class="content-group">
                        <div class="row mt8">
                            <label for="deferred_revenue_journal_id" class="col-lg-5 o_light_label" string="Journal"/>
                            <field name="deferred_revenue_journal_id" domain="[('type', '=', 'general'), ('active', '=', True)]"/>
                        </div>
                        <div class="row mt8">
                            <label for="deferred_revenue_account_id" class="col-lg-5 o_light_label" string="Deferred revenue account"/>
                            <field name="deferred_revenue_account_id" domain="[('account_type', '=', 'liability_current')]"/>
                        </div>
                        <div class="row mt8">
                            <label for="generate_deferred_revenue_entries_method" class="col-lg-5 o_light_label" string="Generate Entries"/>
                            <field name="generate_deferred_revenue_entries_method"/>
                        </div>
                        <div class="row mt8">
                            <label for="deferred_revenue_amount_computation_method" class="col-lg-5 o_light_label" string="Based on"/>
                            <field name="deferred_revenue_amount_computation_method"/>
                        </div>
                    </div>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
